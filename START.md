# 出張旅費精算ツール｜チャット入り口

**新しいチャットを開いたら、まずこのファイルを読ませること。**

---

## これから作るもの（構想）

出張の**旅先の隙間時間**にスマホで入力しておくと、会社の**出張旅費精算の様式に沿った伝票**が
自動で仕上がり、会社PCでは**見て転記するだけ**で済む状態を作るツール。

## なぜ作るか

- 出張は移動の待ち時間が長い（＝入力に使える隙間時間が多い）
- 領収書が日をまたいで分散し、日数が経つほど記憶が薄れる
- 帰社後にまとめて思い出しながら精算するのが心的に重い
- 頻度は高くないが、そのぶん**やり方を忘れて毎回ゼロから思い出す**コストがある

## 踏襲したい「型」（既存アプリで確立済み）

同じ発想で作った**車両日報アプリ「メタろぐ」**が `~/AIアプリ開発/meter-app/` にある。
以下の型が実運用で機能しているので、そのまま踏襲する。

1. **スマホPWA・サーバーなし・localStorage** … 会社PCに一切手を加えない
2. **その場で入力** … 払った瞬間・降りた瞬間に記録し、後から思い出さない
3. **入力の手数を増やさない** … 既定値を持たせ、例外のときだけ入力する
4. **会社様式そのままの伝票を生成** … 合計まで自動計算、脳の変換コストをゼロに
5. **データをURLに内包してメール送信** … サーバーなしでPCへ届ける中核の工夫
6. **印刷にも対応** … 紙で事務職へ渡せば、入力業務ごと委任できる

### 必ず読むべき既存ファイル

| ファイル | 何が学べるか |
|---|---|
| `~/AIアプリ開発/meter-app/report.html` | **最重要**。データをURLハッシュに内包して表を再現する仕組み。ほぼそのまま流用できる |
| `~/AIアプリ開発/meter-app/index.html` | 入力UI（タグ連動の金額入力欄・一時保存・既定値の持たせ方） |
| `~/AIアプリ開発/meter-app/NEXT.md` | 経緯と運用メモ |
| `~/AIアプリ開発/meter-app/zukai.html` | 設計思想（制約の外側で価値を届ける） |

## 車両日報との決定的な違い（設計で最初に効く）

- 車両日報 … **日付が連続する週単位**。毎週必ず発生する
- 出張旅費 … **不定期のイベント単位**（日帰り／1泊2日／2泊3日）。発生頻度が低い

→ データモデルを「週」ではなく「**出張1件**」を軸に組む必要がある。
→ **別アプリ・別URL・別localStorage** として作る（メタろぐには混ぜない）。

## 最初にやること

1. **会社の出張旅費精算フォーマットの写真／PDF** を見せてもらう
   （これがゴール。様式が分かれば、隙間時間に何を入れておけばよいか逆算できる）
2. `grill-me` スキルで**1問ずつ**設計を詰める
   - 誰が・いつ・どこで入力するのか
   - 交通費／宿泊費／日当／接待費など、どの項目があるか
   - 定額のもの（日当・宿泊定額）と実費のものの切り分け
   - 領収書の扱い（メタろぐでは「写真は撮らない」と結論した経緯あり）
   - 承認フロー・提出先・締めのタイミング
3. 合意できてから実装に入る

## 制約・前提

- 会社PC（HCL Notes）には外部ツールを一切入れられない
- サーバー・DB・AI APIは使わない（運用コスト0円を維持する）
- 公開は GitHub Pages を想定
- 卒業制作（8/28発表）の成果物には**含めない**。あくまで別プロジェクト

---

## 新チャットでの最初の一言（コピペ用）

> `~/AIアプリ開発/trip-expense/START.md` を読んでから始めてください。
> 会社の出張旅費精算フォーマットの写真を添付します。
